<Window x:Class="ActorSensor.ReceiverDisplay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wpf="clr-namespace:GMap.NET.WindowsPresentation;assembly=GMap.NET.WindowsPresentation"
        xmlns:vm="clr-namespace:ActorSensor.ReceiverDisplay"  
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    
 

    <Grid>
          <wpf:GMapControl x:Name="MapControl"
                           Zoom="{Binding MapZoom, Mode=TwoWay}"
                           VerticalAlignment="Stretch"
                           HorizontalAlignment="Stretch"
                           />
                          
        <!-- Add a button to toggle map updates -->
        <Button Content="Pause/Resume Map Updates" Command="{Binding ToggleMapUpdateCommand}" VerticalAlignment="Bottom"/>
    </Grid>
</Window>